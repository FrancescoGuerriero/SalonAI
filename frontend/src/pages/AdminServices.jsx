import ServicesPage from "./ServicesPage.jsx";

/*
 * Preserve the legacy administrator URL as a compatibility alias while using
 * the canonical permission-aware service management workspace.
 */
export default function AdminServices() {
  return <ServicesPage />;
}
