# SalonAI AI Service

This directory is reserved for a future Python AI microservice. The current application uses the backend provider abstraction in `backend/src/providers/aiProvider.js`; no separate Python process is required for the present build.

Keep future Python dependencies isolated here with a dedicated virtual environment and API contract. Do not place frontend or Express files in this directory.
